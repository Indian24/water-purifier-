Task: Replace the current Products page with a static, SEO-friendly "Products & Supply" landing page.
Goal: Stop showing dynamic product cards for now; instead show strong trust-building content:
  - Price-match guarantee messaging
  - Short services summary (we supply RO units + spare parts)
  - Full list of available product categories (domestic RO, filters, membrane, electrical parts, pumps, tape, UV light, adapters, faulty parts, etc.)
  - Clear CTAs to contact via WhatsApp (+91 8700762477) or email (ranjanmanish615@gmail.com)
  - Remove/hide product listing UI (search, filters, product cards) so the page acts as an informative supply/ordering page
  - Add LocalBusiness JSON-LD and meta tags for SEO
  - Make the page fully responsive and accessible (mobile-first)
Files to edit (client-side):
  1. client/src/pages/ProductList.tsx (replace this file content with the provided component below)
  2. client/src/hooks/use-products.ts (OPTIONAL: comment out or stub API fetching so the rest of app doesn't attempt to render remote products)
  3. If using server-side routes or server/client fetch (client/src/lib/queryClient.ts or wherever products are fetched), ensure ProductList.tsx no longer depends on that endpoint.
  4. (Optional) Add CSS background images if you will provide images — instructions below.

Replace `client/src/pages/ProductList.tsx` with this component (Tailwind + react-helmet is already available in repo):

--- Paste the block below as the new file contents ---
import React from "react";
import { Helmet } from "react-helmet";

const WHATSAPP_NUMBER = "+918700762477";
const WHATSAPP_LINK = `https://wa.me/918700762477?text=${encodeURIComponent(
  "Hello, I want to buy / enquire about an RO or spare parts. Please contact me."
)}`;
const EMAIL = "ranjanmanish615@gmail.com";

export default function ProductsPage(): JSX.Element {
  return (
    <>
      <Helmet>
        <title>RO Supply & Spare Parts — Price Match Guarantee | AquaShield</title>
        <meta
          name="description"
          content="AquaShield supplies all RO brands, spare parts, membranes, pumps & accessories. Price match guarantee. Contact +91 8700762477 or ranjanmanish615@gmail.com."
        />
        <meta name="robots" content="index, follow" />
        {/* LocalBusiness JSON-LD for SEO */}
        <script type="application/ld+json">
          {JSON.stringify({
            "@context": "https://schema.org",
            "@type": "LocalBusiness",
            name: "AquaShield",
            telephone: WHATSAPP_NUMBER,
            email: EMAIL,
            address: {
              "@type": "PostalAddress",
              streetAddress: "House No. 40, Rajpur Khurd",
              addressLocality: "Chhatarpur",
              addressRegion: "Delhi",
              postalCode: "110068",
              addressCountry: "IN",
            },
            url: window.location.origin,
            description:
              "AquaShield supplies RO units and spare parts across Delhi NCR. Price-match guarantee and doorstep delivery.",
          })}
        </script>
      </Helmet>

      <main className="min-h-screen bg-white">
        <section className="bg-gradient-to-b from-sky-50 to-white py-12 px-6 md:px-12 lg:px-24">
          <div className="max-w-5xl mx-auto">
            <div className="bg-white/60 rounded-2xl p-6 md:p-10 shadow-md">
              <h1 className="text-3xl md:text-4xl font-extrabold text-slate-900">
                RO Supply & Spare Parts — Fast Delivery, Price-Match Guarantee
              </h1>
              <p className="mt-4 text-slate-700 text-base md:text-lg leading-relaxed">
                We provide genuine RO systems, filters, membranes and spare parts for ALL major brands. If you find a lower price — we’ll match it. Contact us via WhatsApp or email and our team will arrange the exact product you want.
              </p>

              <div className="mt-6 flex flex-col sm:flex-row gap-4">
                <a
                  href={WHATSAPP_LINK}
                  target="_blank"
                  rel="noreferrer noopener"
                  className="inline-flex items-center justify-center gap-3 px-5 py-3 rounded-full bg-green-500 hover:bg-green-600 text-white font-semibold shadow"
                  aria-label="Contact on WhatsApp"
                >
                  <svg className="w-5 h-5" viewBox="0 0 24 24" fill="none" aria-hidden>
                    <path d="M..." fill="currentColor" /> {/* icon placeholder */}
                  </svg>
                  WhatsApp: {WHATSAPP_NUMBER}
                </a>

                <a
                  href={`mailto:${EMAIL}`}
                  className="inline-flex items-center justify-center gap-3 px-5 py-3 rounded-full border border-slate-200 bg-white hover:bg-slate-50 text-slate-900 font-semibold shadow-sm"
                  aria-label="Contact by email"
                >
                  Email: {EMAIL}
                </a>
              </div>
            </div>

            {/* Price Match + Benefits */}
            <div className="mt-8 grid grid-cols-1 md:grid-cols-3 gap-6">
              <div className="p-6 bg-white rounded-xl shadow">
                <h3 className="font-bold text-lg">Price Match Guarantee</h3>
                <p className="mt-2 text-slate-600 text-sm">
                  Find a lower price on the same genuine product and we'll match it. We verify authenticity & warranty before matching.
                </p>
              </div>

              <div className="p-6 bg-white rounded-xl shadow">
                <h3 className="font-bold text-lg">All Brands Supported</h3>
                <p className="mt-2 text-slate-600 text-sm">We source parts for KENT, Aquaguard, Livpure, AO Smith and every other brand.</p>
              </div>

              <div className="p-6 bg-white rounded-xl shadow">
                <h3 className="font-bold text-lg">Doorstep Delivery</h3>
                <p className="mt-2 text-slate-600 text-sm">Same-day dispatch within Delhi NCR when stock permits. Installation available on request.</p>
              </div>
            </div>

            {/* Available Products List */}
            <section className="mt-10 bg-white rounded-xl p-6 shadow">
              <h2 className="text-2xl font-bold text-slate-900">Products & Spare Parts We Supply</h2>
              <p className="mt-2 text-sm text-slate-600">We supply genuine parts, spares and full RO units. If you don't see your item listed, contact us — we'll source it.</p>

              <ul className="mt-4 grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 gap-4 list-none">
                {[
                  "Domestic RO units (all brands)",
                  "Replacement filters (Carbon / Sediment / Spun)",
                  "RO Membranes (TDS-rated)",
                  "RO Pumps",
                  "Solenoid valves",
                  "Adapters & Fittings",
                  "Electrical parts (PCBs, sensors)",
                  "UV lamps & adapters",
                  "Faulty parts & repair kits",
                  "Complete filter kits (Carbon+Sediment+Spun)",
                ].map((item) => (
                  <li key={item} className="bg-slate-50 rounded-lg p-4">
                    <h3 className="font-medium text-slate-800">{item}</h3>
                    <p className="mt-2 text-sm text-slate-600">Available for all major brands — call or WhatsApp for exact model availability & pricing.</p>
                  </li>
                ))}
              </ul>

              {/* Simple spare list CTA */}
              <div className="mt-6 border-t pt-6 flex flex-col sm:flex-row items-start sm:items-center justify-between gap-4">
                <div>
                  <p className="text-slate-700">
                    Need a specific spare part or membrane? Send a photo on WhatsApp (<strong>{WHATSAPP_NUMBER}</strong>) or email us at <strong>{EMAIL}</strong>.
                  </p>
                </div>

                <div className="flex gap-3">
                  <a
                    href={WHATSAPP_LINK}
                    className="px-4 py-2 rounded-md bg-green-500 text-white font-semibold"
                    target="_blank"
                    rel="noreferrer"
                  >
                    Message on WhatsApp
                  </a>

                  <a
                    href={`mailto:${EMAIL}`}
                    className="px-4 py-2 rounded-md border border-slate-200 text-slate-800"
                  >
                    Email Us
                  </a>
                </div>
              </div>
            </section>

            {/* FAQ / How it works */}
            <section className="mt-10">
              <h2 className="text-xl font-bold">How ordering works</h2>
              <ol className="mt-3 list-decimal ml-5 text-slate-600">
                <li>Tell us what you need (WhatsApp photo or model number).</li>
                <li>We confirm price, warranty & shipping time.</li>
                <li>We dispatch same-day / next-day within Delhi NCR when in stock.</li>
                <li>We offer installation & AMC (optional).</li>
              </ol>
            </section>

            <footer className="mt-10 text-sm text-slate-500">
              <p>
                Note: This page is a supply & enquiry landing. Product catalogue & purchasing flow will return when full inventory sync is re-enabled.
              </p>
            </footer>
          </div>
        </section>
      </main>
    </>
  );
}
--- end replacement ---

Optional small change (prevent product API calls while page switched):
- If the app has a hook `client/src/hooks/use-products.ts` or `client/src/lib/api.ts` that auto-fetches `/api/products`, open it and either:
  - Return an empty array (quick stub) like `return { products: [], isLoading: false };`
  - OR mark it to short-circuit when on ProductList route (but easiest: stub the hook to avoid network noise).

Accessibility & responsiveness:
- The provided component is mobile-first with Tailwind grid. Verify at breakpoints:
  - 375px (mobile), 640px (sm), 768px (md), 1024px (lg)
- All actionable CTAs use anchors with accessible labels and open WhatsApp/email.

SEO & Analytics additions:
- react-helmet included to set title/meta & JSON-LD snippet.
- Add the page to sitemap.xml (if you maintain sitemap, add `/products`).
- Add Open Graph meta (optional) for social sharing.

Replace/remove product-cards & filters:
- Remove or comment out the component that renders product grid on the `/products` page (most likely `client/src/components/ProductCard.tsx` is referenced by ProductList). Since we replaced ProductList.tsx entirely, those components will no longer be used there — you can keep them in repo for future reuse.
- Optionally delete UI elements like header badge saying "temporary dev preview" in Replit — not required for code.

If you plan to supply background images:
- Add them to `client/public/assets/bg-hero.jpg` (or `src/assets`) and add to the hero section wrapper classes:
  - Add `style={{ backgroundImage: 'url(/assets/bg-hero.jpg)', backgroundPosition: 'center', backgroundSize: 'cover' }}` to the outer hero `section` or use Tailwind `bg-[url('/assets/bg-hero.jpg')]` if configured.
- Keep opacity overlays to ensure text contrast.

Testing checklist to run after changes:
1. Build & run dev server: `npm run dev` (client folder if separate)
2. Visit `/products` on desktop and mobile viewport verify:
   - WhatsApp link opens mobile or web WhatsApp with prefilled message
   - Email link opens mail client
   - Page renders without errors (no missing variables)
3. Check meta tags with view-source
4. Validate JSON-LD using Google Rich Results test
5. Confirm there are no failing console errors for product hooks
6. Add `/products` to sitemap and ping Google Search Console

Notes for backend (no required changes):
- No backend change is necessary unless the frontend was tightly coupled to product API. If so stub the hook server-side or return empty results.

If you want, I can:
- Provide the exact small patch to stub `use-products` in your repo
- Create the `index.html`/meta propagation or update `client/src/main.tsx` to include Helmet provider if needed

End of instruction.
